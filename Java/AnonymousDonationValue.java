// Anonymous Donation value selected

//------------------------------------------------------------------------------
// isAnonymousDonation check if option selected is true/false
public boolean isAnonymousDonation(anonymousvalue) {
  // getAnonymousValue retrieves the anonymousdonation selected value 
  anonymousvalue = getAnonymousValue(anonymousDonation);
	// If anonymousvalue value is false check session status
	if (!anonymousvalue) {
		// Call method
		handleApiRequest; 
	return false;
  }
}

//------------------------------------------------------------------------------
